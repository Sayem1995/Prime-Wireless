{
  "_comment": [
    "Default theme palette - the SINGLE SOURCE OF TRUTH for brand colours.",
    "These are Prime Wireless's values (navy / white / electric blue). A",
    "deployment can override them with BRAND_* environment variables; see",
    ".env.prime.example.",
    "",
    "Loaded by tailwind.config.js (Node `require`) and vite.config.ts (JSON",
    "import). The same defaults are mirrored as a TypeScript constant in",
    "contracts/brand.ts, because that module is also bundled for the BROWSER,",
    "where both ways of reading this file fail: `createRequire` is a Node",
    "builtin that Vite externalizes, and a `.cjs` module is evaluated by Vite's",
    "SSR loader with no `module` binding. api/brand-colors.test.ts asserts the",
    "two copies stay identical, so drift cannot go unnoticed."
  ],
  "primary": {
    "DEFAULT": "#0B2545",
    "dark": "#071A33",
    "light": "#13315C"
  },
  "secondary": {
    "DEFAULT": "#DCE7F5",
    "dark": "#B9CDE8",
    "light": "#F2F7FD"
  },
  "surface": "#FFFFFF",
  "ink": "#0A1A2F",
  "accent": "#1E90FF"
}